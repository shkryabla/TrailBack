<?xml version="1.0" encoding="utf-8"?>
<!-- ИЗМЕНЕНО: корень был одиночным TextView, стал LinearLayout с иконкой
     слева — см. MenuActivity/MenuAdapter (иконки пунктов меню подобраны из
     Google Material Symbols, google/material-design-icons, Apache 2.0). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground">
    <ImageView
        android:id="@+id/iconImage"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="20dp"
        android:tint="@color/text_white"
        tools:src="@drawable/ic_flag" />
    <TextView
        android:id="@+id/titleText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/text_white"
        android:textSize="16sp"
        tools:text="Пункт меню" />
</LinearLayout>
